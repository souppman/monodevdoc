# MONOREPO ENVIRONMENT STRUCTURE GUIDE
# -----------------------------------
# In this monorepo, environment variables are isolated by service.
# Do NOT put service-specific secrets (like DB URLs or API Keys) here.
# Instead, create a .env file in the specific service directory.

# 1. RAG Service (Python)
# Location: services/rag-service/.env
# Required for: OpenAI/DeepSeek/OpenRouter, Pinecone
# content:
#   LLM_API_KEY=...
#   PINECONE_API_KEY=...

# 2. Journal Service (Node.js)
# Location: services/journal-service/.env
# Required for: Supabase/Prisma, GitHub Webhooks
# content:
#   DATABASE_URL="postgresql://..."

# 3. Frontend & BFF (Next.js)
# Location: apps/web/.env.local AND services/bff/.env.local
# Required for: Public API endpoints, Authentication
# content:
#   NEXT_PUBLIC_API_URL=...

# This root file is only for shared repo-wide tooling (if any).
# CURRENT_ENV=development
